# R04R.R #

# Array #

a1 <- array(c(1:12), dim=c(4,3))
a1
class(a1)

a2 <- array(c(1:12), dim=c(2,2,3))
a2
class(a2)

# a1이랑 a2 class가 다른 이유? a1 => 2차, a2 => 3차

# a2에서 3, 6, 12 출력

cat(a2[1,2,1], a2[2,1,2], a2[2,2,3])
cat(a2[2,1,1:3])    # or cat(a2[2,1,])
cat(a2[2,1,1], a2[2,2,1], a2[2,1,2], a2[2,2,2], a2[2,1,3], a2[2,2,3]) # a2[2,,]

